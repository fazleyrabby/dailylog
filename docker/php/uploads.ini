; Raise upload limits so note image/screenshot uploads aren't rejected by PHP
; before reaching Laravel validation (default upload_max_filesize is 2M).
upload_max_filesize = 12M
post_max_size = 16M
memory_limit = 256M
